# Route UF output through the local tee listener
# The tee forwards to the real indexer defined in config.yaml

[tcpout]
defaultGroup = etairos_tee

[tcpout:etairos_tee]
server = 127.0.0.1:19997
useACK = true
